name: Renovate Test
on:
  - push
  - pull_request
  - workflow_dispatch

jobs:
  renovate-test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '8.0.x'
